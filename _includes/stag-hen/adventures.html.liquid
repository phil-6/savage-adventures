<section class="container-fluid adventures-summary">
  {% for adventure in site.data.adventures.adventures %}
    {% if adventure.stag-hen %}
      {% capture loop %}{% cycle 'stag-odd', 'stag-even' %}{% endcapture %}
      <div class="row">
        {% if loop == 'stag-even' %}<div class="col-md-5 d-none d-md-block"></div>{% endif %}
        <div class="{% if loop == 'stag-even' %}col-md-7{% endif %}">
          {% include adventures/title-row.html.liquid %}
        </div>
      </div>
      <div class="row">
        <div class="col-md-5 {% if loop == 'stag-odd' %}order-md-2{% endif %}">
          <img src="/assets/images/{{ adventure.image }}" alt="{{ adventure.name }}" loading="lazy">
        </div>
        {% include adventures/summary-description.html.liquid %}
      </div>
    {% endif %}
  {% endfor %}
</section>
